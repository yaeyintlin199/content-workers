# Enable automatic installation of peer dependencies
auto-install-peers = true

# @content-workers scope configuration
# Requires NPM_TOKEN environment variable for publishing
# See PUBLISHING.md for setup instructions
@content-workers:registry=https://registry.npmjs.org/
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
